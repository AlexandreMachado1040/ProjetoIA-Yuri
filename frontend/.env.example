# URL do Cloudflare Worker "motor-pvsyst".
# Opcional: o código já usa esta URL como padrão. Defina aqui apenas se
# quiser apontar para outro ambiente (ex.: backend local ou outro worker).
#
# Copie este arquivo para `.env` (ou `.env.local`) e ajuste se necessário.

VITE_WORKER_URL=https://motor-pvsyst.alexandreclm.workers.dev
